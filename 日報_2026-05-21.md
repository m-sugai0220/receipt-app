# 作業日報 2026-05-21

## 前回（2026-05-20）からの引き継ぎ

| 優先度 | タスク | 状態 |
|---|---|---|
| 高 | 他のレシートのOCR精度検証 | レシート忘れ → 次回へ持ち越し |

---

## 本日の作業内容

### ① 検索・絞り込み（`app/page.tsx`）

- [x] 店名・メモでリアルタイムフィルタリング（全タブ対応）
- [x] ×ボタンでクリア、ヒット件数表示

### ② 月別グラフ（`app/page.tsx`）

- [x] 月別タブの上部に「支出推移」バーグラフを追加
- [x] 最大月を基準にした横棒グラフ（件数・金額も表示）

### ③ OCR確認モーダル（`app/page.tsx` + `app/api/ocr/route.ts`）

- [x] OCR後に自動保存せず、確認・編集モーダルを表示
- [x] 店名・金額・日付・勘定科目・メモを保存前に修正可能
- [x] レシート画像サムネイルをモーダル内に表示
- [x] APIからDB insertを削除、フロントエンドから直接Supabaseに保存する方式に変更

### ④ メモ欄追加（`app/page.tsx` + `lib/supabase.ts`）

- [x] Receipt型に `memo` フィールドを追加
- [x] OCR確認モーダル・インライン編集に入力欄を追加
- [x] 一覧行に📝アイコンつきでメモを表示
- [x] CSV出力にメモ列を追加
- [x] **Supabase側SQL実行済み:** `ALTER TABLE receipts ADD COLUMN IF NOT EXISTS memo TEXT;`

### ⑤ 複数レシート一括削除（`app/page.tsx`）

- [x] 「選択」ボタンでチェックボックスモードに切り替え
- [x] 選択件数が表示され「削除（N件）」ボタンで一括削除

### ⑥ ログイン機能（`app/login/page.tsx`）

- [x] Supabase Authによるメアド＋パスワードログイン
- [x] 未ログイン時は `/login` にリダイレクト
- [x] ナビバーにログアウトボタンを追加
- [x] Supabase Site URLをVercel本番URLに設定済み
- [x] ユーザー作成・ログイン確認済み

### デプロイ

- [x] Vercelへデプロイ済み（`fd36b3e` → main push）
- [x] 本番ログイン確認済み

---

## 明日の作業

### 優先度：高
- [ ] **他のレシートのOCR精度検証** — レシートを持参して撮影・精度確認・追加対応

### 優先度：低（余裕があれば）
- [ ] （特になし）

---

## 懸念点・メモ
- OCRキャンセル時はStorage上の画像が残る（孤立ファイル）。件数が増えたら定期クリーンアップを検討
- 既存レシートはmemoカラムがNULLのため表示なし（新規保存分から有効）
